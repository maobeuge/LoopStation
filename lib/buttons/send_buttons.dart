import 'package:flutter/material.dart';

class SendButtons extends StatelessWidget {
  const SendButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
